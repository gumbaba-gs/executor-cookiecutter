{
    "Account" : {
        "Id" : "{{cookiecutter.account_id}}",
        "Name" : "{{cookiecutter.account_name}}",
        "Seed" : "{{cookiecutter.account_seed}}"
        {% if cookiecutter.account_type == "aws"  %}
        ,
        "ProviderId" : "{{cookiecutter.provider_account_id}}"
        {% endif %}
    }
}
